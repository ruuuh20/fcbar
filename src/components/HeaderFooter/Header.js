import React from 'react';
import PropTypes from 'prop-types';

const Header = ({}) => {
  const handleClick = () => {};
  return (
    <div onClick={handleClick}>
    Header</div>
  );
}

// Header.propTypes = {
//   : PropTypes.
// };

export default Header;
